package com.github.sebhoss.yosql.error;

/**
 * Error reporting should be in its own package because users can redirect log messages from that package into its own log file.
 */
public class ErrorReporter {
}
