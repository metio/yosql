/*
 * SPDX-FileCopyrightText: The yosql Authors
 * SPDX-License-Identifier: 0BSD
 */

-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
-- parameters:
--   userId: int
SELECT *
FROM person
WHERE id = :userId
;
